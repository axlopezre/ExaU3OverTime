import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ExU3OverTime',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
